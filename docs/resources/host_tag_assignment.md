---
page_title: "qualys_host_tag_assignment Resource - terraform-provider-qualys"
subcategory: ""
description: |-
  Manages which asset tags are assigned to a host asset.
---

# qualys_host_tag_assignment (Resource)

Manages which asset tags are assigned to an AssetView/CSAM host asset
(Confirmed: `POST /qps/rest/2.0/update/am/hostasset` with
`tags.add`/`tags.remove`, this repository's discovery notes doc 03 §14).
Tag-based scan and report targeting depends on this association.

One resource manages the full set of `tag_ids` on one `host_asset_id`
authoritatively: on every apply it reconciles by adding tags present in
configuration but not currently assigned, and removing tags assigned but no
longer in configuration.

## Example Usage

```terraform
resource "qualys_asset_tag" "pci_scope" {
  name = "pci-scope"
}

resource "qualys_host_tag_assignment" "server01" {
  host_asset_id = "1234567"
  tag_ids       = [qualys_asset_tag.pci_scope.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- **host_asset_id** (String) AssetView/CSAM host asset ID (`qwebHostId`) to assign tags to. Changing it forces replacement.
- **tag_ids** (Set of String) Asset tag IDs assigned to this host asset. Managed authoritatively.

### Optional

- **id** (String) The ID of this resource.

## Import

```shell
terraform import qualys_host_tag_assignment.server01 1234567
```
